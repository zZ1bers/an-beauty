# Copy to /opt/an.beauty/.env on the VPS (never commit real values)

DOMAIN=an-beauty.com

POSTGRES_USER=anbeauty
POSTGRES_PASSWORD=change-me-strong-db-password
POSTGRES_DB=anbeauty

JWT_SECRET=change-me-long-random-jwt-secret

# Same origin via Caddy → nginx → /api
CORS_ORIGIN=https://an-beauty.com
PUBLIC_URL=https://an-beauty.com/api
FRONTEND_URL=https://an-beauty.com

# Password-reset emails (IONOS mailbox)
SMTP_HOST=smtp.ionos.de
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=info@an-beauty.com
SMTP_PASS=change-me-mailbox-password
MAIL_FROM="AN.Beauty <info@an-beauty.com>"

# SMS via Twilio (confirm + day-before reminder). Leave empty to skip SMS in logs.
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM=+49xxxxxxxx or approved sender ID
# SALON_ADDRESS=Rückerstr. 4, 90419 Nürnberg

# Only for ONE-TIME seed on the server (not used by compose automatically)
ADMIN_EMAIL=admin@an.beauty
ADMIN_PASSWORD=change-me-strong-admin-password
